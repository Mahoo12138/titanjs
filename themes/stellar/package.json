{
  "name": "titan-theme-stellar",
  "version": "0.1.0",
  "description": "Stellar theme for TitanJS - ported from hexo-theme-stellar",
  "type": "module",
  "main": "./dist/theme.config.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "preact": "^10.19.0",
    "preact-render-to-string": "^6.3.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@titan/types": "workspace:*",
    "@titan/core": "workspace:*",
    "tsup": "^8.0.0"
  }
}
